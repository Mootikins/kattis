#include <iostream>
#include <string>
#include <vector>

using namespace std;

int int main( int argc, char* argv[] ) {
	string recording;
	int cases;
	vector<string> sounds;
	string currsound;

	cin >> cases;

	for ( i = 0; i < cases; i++ ) {
		getline( cin, recording );

		while ( currsound != "the" ) {
			cin >> currsound;
			cin >> currsound;
			cin >> currsound;
		}
	}

	return 0;
}
